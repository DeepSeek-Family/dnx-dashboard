/**
 * Canonical localStorage key for the JWT access token.
 * Import this (or `TOKEN_STORAGE_KEY` from `@/utils/local-storage`) anywhere the token is read or written.
 */
export const AUTH_ACCESS_TOKEN_STORAGE_KEY = 'token' as const
